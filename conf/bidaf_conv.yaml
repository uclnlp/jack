description: >
  Convolutional BiDAF reader as described in https://openreview.net/pdf?id=HJRV1ZZAW.

parent_config: './conf/bidaf.yaml'

# 'lstm', 'gru', 'sru' (simple recurrent unit), 'gldr' (gated linear dilated residual network)
encoder: 'gldr'
